#include<stdio.h>
int main()
{
    int age;
    printf("enter the age of a person");
    scanf("%d",&age);
if(age>=18)
{
    printf("a person eligible for voting");
}
else
{
    printf("a person is not eligible for voting");
}
}
